
public class Config {
	
	static boolean debug = true; // Standard Debug(Block placed,Key pressed etc.)
	static boolean debugMap = false; // Map Debug(Printing Map To Console upon load and changes)
}
